import React from "react";
import Header from "./Header.js";
import Footer from "./Footer.js";

// Not sure if this should be a functional component or a class-based one
// User will only use this once to create a username and password - most likely going to be functional

const SignUp = () => {

  return (
    <div>
    </div>
  );
};


export default SignUp;
