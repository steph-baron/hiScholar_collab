import React, { Component } from "react";
import Header from "./Header.js";
import Footer from "./Footer.js";

// this is not a stateful page - the child components inside it are stateful but not the page over all?
// the point of this page is to display the user's personal info, school & program preferences, and school twitter feed

class UserProfile extends Component {

}


export default UserProfile;
